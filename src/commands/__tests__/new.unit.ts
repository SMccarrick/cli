it.todo("running new command with a name creates folder");
it.todo(
  "running new command without a name throws an error and tells you to add a name"
);
it.todo("shows a loader when creating a successful repo");
it.todo(
  "running new command checks if a folder in the current directory has the same name and generates an error"
);
